<!--Strona główna, sprawdzone w3-->
<!DOCTYPE html>
 <html lang ="pl-PL">
 <head>
    <link rel="stylesheet" href="/style.css">

    <meta charset="UTF-8">
    <title>System egzaminacyjny urzędu lotnictwa cywilnego</title>
</head>
<body>
    <header>
        <h1><%= __('nav.header') %></h1>
        <img src="../../images/lotnictwo.png" alt="SEULC Logo">
        <script type="application/javascript" src="../../js/validationCommon.js"></script>
    </header>


    <nav>
        <ul>
            <li><a href="/" class="<%= navLocation == 'main' ? 'active' : ''%>"><%= __('nav.main-page') %></a></li>

            <% if (loggedUser) { %>
                <li><a href="/Egzamin"class="<%= navLocation == 'exam' ? 'active' : ''%>"><%= __('nav.exam') %></a></li>
                <li><a href="/Pytanie_egzamin"class="<%= navLocation == 'anExam' ? 'active' : ''%>"><%= __('nav.questionExam') %></a></li>
                <li><a href="/Pytanie"class="<%= navLocation == 'question' ? 'active' : ''%>"><%= __('nav.question') %></a></li>
                <li><a href="/Osoba"class="<%= navLocation == 'person' ? 'active' : ''%>"><%= __('nav.person') %></a></li>
                <li><a href="/changeLang/pl" class="<%= lang == 'pl' ? 'active' : ''%>">PL</a></li>
                <li><a href="/changeLang/en" class="<%= lang == 'en' ? 'active' : ''%>">EN</a></li>
            <% } %>
        </ul>

        <% if(!loggedUser) { %>
                 <div id="login">
                      <h5><%= __('login.login') %></h5>
                      <form method="post" action="/login" novalidate>
                        <label for="loginPesel"><%= __('login.login') %></label>
                        <input type="number" id="loginPesel" name="pesel">

                        <label for="loginPassword"><%= __('login.pass') %></label>
                        <input type="password" id="loginPassword" name="password">

                        <input type="submit" value=<%= __('login.loginBTN') %>
                        <span id="loginErrors"><%= loginError ? loginError : ''%></span>
                      </form>
                 <div/>
            <% } else { %>
                 <span> <%= __('login.loginBy') %> <%= loggedUser.firstName + " " + loggedUser.lastName %> </span>
                 <a href="/logout"> <%= __('login.logout') %></a>
            <% } %>


    </nav>


